.alert-box {
  min-width: 200px;
  margin: 0 auto;
  padding: 10px;
  border: 1px solid #d6d6d6;
  border-radius: 5px;
  background-color: #f9f9f9;
  position: relative;
  animation: popup-alert 0.5s ease-out;
}

@keyframes popup-alert {
  from {
    opacity: 0;
    transform: translateY(10px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

.alert-box.show {
  display: block;
  animation: fadeOut 3s ease-out forwards;
}

@keyframes fadeOut {
  from {
    opacity: 1;
  }
  to {
    opacity: 0;
  }
}
